SET LINESIZE 1000
SET PAGESIZE 0
SET FEEDBACK OFF
SET ARRAYSIZE 100
SET TRIMSPOOL ON
SET TIMING ON
SPOOL export.nt

SELECT
  S$RDFTERM || ' ' ||
  P$RDFTERM || ' ' ||
  O$RDFTERM || ' .'
FROM TABLE(SEM_MATCH('
SELECT ?s ?p ?o
WHERE { ?s ?p ?o }
',
SEM_MODELS('ICGC_RDFVIEW'),
null,
null,
null,
null,
' PLUS_RDFT=VC DO_UNESCAPE=T '))
;

SPOOL OFF

EXIT
